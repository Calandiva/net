/* ═══════════════════════════════════════════════════════════════════════════
   99-boot.js — 마지막에 실행. 앞선 모든 모듈(특히 const 로 선언된 TUT 등)이
   초기화된 뒤여야 boot() 안의 afterEdit() 이 안전하다.
   ═══════════════════════════════════════════════════════════════════════════ */
boot().catch(err=>{
  console.error('부팅 실패', err);
  try { toast('초기화 중 문제가 발생했습니다: ' + err.message, 'bad'); } catch(_){}
});
